# PetRival · 宠物擂台

## 当前统一版本

当前线上 **Sites v17 已发布成功**：[小精灵跳跳课](https://petrival.clear-oasis-2741.chatgpt.site/jump) 已升级为真实大模型决策、示范上下文和复杂关卡生成。207项自动测试通过；本地真实模型生成关验证通过，并在网页拿钥匙开门，尚未验证整关通关或稳定学习增益。原有小院、对话、手机号身份、推箱子和打拳保留。详情见 [模型版说明](docs/JUMP-MODEL-2026-09-15.md)。

线上站点：https://petrival.clear-oasis-2741.chatgpt.site/

概念介绍：[/about.html](https://petrival.clear-oasis-2741.chatgpt.site/about.html)，从首页「认识 PetRival」进入。介绍页包括交互式挑战流程示意、推箱子与打拳、日常陪伴和比赛技能；图示用于解释概念，不是实际对局。

本仓库根目录是统一源码，包含宠物小院、自然对话、等级与技能、像素编辑、推箱子和打拳，以及 Sites/D1 持久存档和积分流水。游戏通过对话影响宠物生活，不使用生活行为控制按钮。

- 本地 Node 服务：`npm start`，存档在本地服务的 `data/`。
- Sites：`npm run build` 生成 Worker、网页资源和既有 D1 迁移；发布遵循 [Sites 部署说明](docs/SITES.md)。
- 验证：`npm test`、`npm run test:cloud`、`npm run check`。
- 登录现状：支持 CloudBase 手机短信登录、游客存档绑定，以及 Sites 身份/游客 Cookie。邮箱验证码尚未接入；游客尚未绑定账号时仍依赖浏览器 Cookie。
- 历史游戏整合基线：2026-09-14，GitHub `b995ae8`，Sites **v11 已发布成功**。推箱子与打拳共用宠物和存档，分别展示比赛记录与排名；比赛技能槽为 1 个，描述与代码合计最多 100 token。
- 推箱子默认使用 `MODEL_PUSH_POLICY=feedback`；候选预演 `preview` 已实现但需显式开启。
- 交付和验证：[整合记录](docs/claims/2026-09-14-multi-game-integration.md)、[拳台规则](docs/BOXING-MVP-2026-09-14.md)、[比赛技能](docs/COMPETITION-SKILL-2026-09-14.md)。本次合计 181 项自动测试通过，不等于生产真实模型全功能实玩验收。
- 后续开发先阅读 [协作约定](CONTRIBUTING.md) 和 [任务规则](AGENTS.md)，从最新 `origin/main` 建立独立工作分支，不能继续修改旧交付副本。


**宠物提前备好题，人和宠物一起挑战别人的宠物。**

独立像素风网页游戏。推箱子采用提前备题、挑战即开；打拳在人宠共用页面打开，各自独立应战。两种游戏均有服务端规则与独立排名。

## 横版示范学习试玩

新增「小精灵跳跳课」，在游戏小屋打开，也可访问 `/jump.html`。你和小精灵一起行动，你负责示范，金币和机关只能由它触发。当前源码升级为真实大模型分段决定按键：卡住可回去录制示范，再交给模型参考。关卡包含高台、钥匙、回头开关、锁门和金币支路。模型可在后台备下一关，完整物理回放验证通过后，由玩家点击切换。

这是大模型上下文示范学习，不是权重训练或完整玩家个性训练。笔记仅保存在当前浏览器，按已有宠物区分；不计积分。闯关与备题会产生真实模型 Token 用量；模型不可用时暂停精灵，真人仍可操作。详见 [模型版行为与验证](docs/JUMP-MODEL-2026-09-15.md)。

## 当前可用

- 领养一只宠物；每只宠物常备一张经过可解性验证的关卡；初始 8×8、双箱子，出题能力升级后解锁最大 10×10、四箱子。
- 默认像素宠物“小精灵”，也可选择芽芽灵、火花狐、云朵兽；支持逐像素编辑、改名、JSON / PNG 导入导出。
- 从宠物列表选择对手，立即使用双方缓存关卡开局。新题在后台生成，成功验证后才替换旧题；进行中的对局保持版本不变。
- 左侧宠物 AI、右侧真人同时挑战对方的题，各自操作和计分。AI 思考时真人即可操作，左侧实时显示已执行步骤。自己的守擂关也能人宠同时试玩，不计排名。
- 默认无思考、逐步行动：普通人宠试玩和宠物挑战都按次选择推箱子，走路工具负责到位；不再需要单独选择试验入口。[协议实测](docs/STEP-PLAY-TRIAL-2026-09-13.md)。
- 方向键 / WASD 移动，Z 撤销，R 重来，也有屏幕方向按钮。
- 操作回放由服务端重放验证；胜负、分数、时间由服务端计算，不信任浏览器上报的“已通关”。
- 刷新后保留本地未提交操作；服务重启后保留身份、关卡、计时和历史成绩。
- 训练宠物用于一个人试体验，明确标注、不冒充真人、不进入正式排行榜。

当前交付包含**本地 Node 原型和已发布的 Sites 网站**。历史真实 DeepSeek 生成、闯关及浏览器样本见 [验收记录](docs/DEEPSEEK-PIXEL-ACCEPTANCE.md)，只代表对应版本和样本。v11 的云端响应测试验证了模型请求等待期间真人输入仍可处理；拳台独立本机进程曾报告偶发卡顿，尚未确认根因，不能据此声称已修复。

## 启动

需要 **Node.js 24 或更新版本**，使用锁文件安装依赖。

```sh
git clone --branch main https://github.com/FromNextWorld/PetRival.git
cd PetRival
npm ci
npm start
```

打开 <http://127.0.0.1:4388>。默认只监听本机。

本地默认端口为 **4388**，可以通过 `PORT` 指定其他端口。复制 `.env.example` 为 `.env` 并填写自己的 Key 后重启，即可使用 DeepSeek Pro；未配置 `.env` 时使用算法模式。

在两个不同浏览器或普通 / 无痕窗口中，各领养一只宠物，就能测试两个独立主人的异步对抗。同一浏览器的普通标签页共享访客身份。

```sh
npm test
npm run check
```

`npm run check` 检查 JavaScript 语法和常见密钥模式；不是完整安全审计。当前没有配置 GitHub Actions 或分支保护硬门禁。

## 推箱子挑战与排名规则

1. 领养时勾选异步守擂，表示允许其他宠物直接发起挑战。对方不用同时在线。
2. 一场对局包含两张锁定版本的关卡；A 的主人和宠物挑战 B 的题，B 的主人和宠物挑战 A 的题。自家题的练习不计入正式成绩。
3. 真人点击进入挑战才开始自己的 3 分钟计时。撤销、重来、关闭和刷新都不重置计时。
4. 每次通关 **+100**，失败 / 认输 / 超时 **−20**。一方的分数是主人与宠物之和。模型返回非法操作也算宠物失败。
5. 同分比较计分用时。真人用服务端经过时间；宠物包括排队、思考、实际执行的总耗时，每条动作按 220ms 执行，时间只计算一次。失败统一按 180 秒计入比较，不能靠秒退获得时间优势。
6. 双方主人与宠物全部完成后，才结算一次并更新宠物累计分、胜负和排行榜。同分按累计计分用时排序。这是简单原型积分，不是经过平衡的竞技评级。
7. 玩家可以边玩边看自己宠物的实时步骤；完成后也能看完整回放。对手路线在自己结束前保密。参赛宠物只收到当前地图，不收到出题证明、其他宠物的对话或真人操作。
8. 未在 24 小时内完成的对局作废，不计分。模型服务掉线、服务重启打断 AI 等基础设施故障也作废并保留已执行记录，不重置计时后重赛。模型思考超过比赛时限、给错操作或未解出则是正常失败。
9. 同一对手同时只能有一场有效挑战，每只宠物同时最多三场；同一组关卡版本不能重复计分。

训练宠物没有真人主人，所以训练赛只显示体验分，不虚构对方真人成绩，也不改变排行榜。

## AI 模式

### 无密钥的算法模式（默认）

程序化反向拉箱生成多个候选，用独立的有界最少推箱搜索验证，再按推箱次数、解法中的换箱与离开目标次数择优。参赛宠物重新独立搜索，并由统一游戏引擎执行操作。

这是**经典算法 AI，不是大语言模型**。标准 / 困难选项及“难 / 绕”关键词改变生成参数，不具备任意自然语言设计理解；也尚未接入外部的关卡好玩优化研究框架。

### DeepSeek Pro 模式

将 `.env.example` 复制为本机 `.env`，在编辑器或密钥管理环境中填写：

```dotenv
AI_MODE=model
MODEL_CHAT_URL=https://api.deepseek.com/chat/completions
MODEL_NAME=deepseek-v4-pro
MODEL_API_KEY=your-local-secret
MODEL_TOKEN_PARAMETER=max_tokens
MODEL_PLAY_EFFORT=none
```

不要把真实密钥发到聊天、写进前端或提交到 Git。此仓库默认忽略 `.env` 与运行数据。

DeepSeek 出题固定使用高思考，闯关由 `MODEL_PLAY_EFFORT=high|low|none` 控制，默认是 `none`。`none` 关闭闯关思考并默认使用逐步推动协议；high/low 保留整段规划。无思考时，模型仍为 Pro；三档均使用 `max_tokens: 16384`。修改配置后需要重启服务才会用于新运行，正在进行的挑战不应为换配置而中断。Key 仅由服务端读取，不返回给浏览器或写入外观文件。算法求解器只负责独立验证关卡；比赛动作来自模型本次的规划。

也保留兼容 Chat Completions 供应商适配：要求支持 `messages`、`response_format: {type: "json_object"}` 和 `choices[0].message.content`；可将 Token 参数改为 `max_completion_tokens`。这不等于所有供应商都已测试。

- 出题：给模型可编辑的关卡骨架和用户要求；返回结构化地图。失败时提供验证错误并最多修复一次。仍未通过就保留旧关，不偷偷替换成算法题并声称模型成功。
- 闯关：只给当前地图，每次最多 120 个方向动作、最多三轮规划。真实引擎判定结果，不采信模型自称通关。
- 调用上限：出题请求最多 240 秒；AI 整场闯关最多 180 秒，包含排队、规划和执行。闯关最多并行 2 个、备题最多并行 1 个，分别排队且各最多 16 个等待任务；后台备题不占用闯关通道。搜索任务单次 15 秒，并行 2 个、队列最多 32 个。
- 第一次领养时先分配已验证的算法入门关，让玩家立即可玩；配置的模型在后台生成替代关卡。每一关标明实际来源。
- 外部模型供应商将收到设计要求和关卡数据。当前没有将反馈用于训练、出售数据或后台收集用户私人内容的功能。

协议参考：[DeepSeek 官方接入文档](https://api-docs.deepseek.com/zh-cn/)及[思考模式参数](https://api-docs.deepseek.com/guides/thinking_mode/)。一次成功样本不代表所有关卡都能在时限内解出。

### 同题思考强度评测

固定样本保存在 `test/fixtures/play-effort-levels.json`，包含 8 张既有模型关和 2 张训练关。运行下面的命令会使用本机 `.env` 中的 Key，真实调用 Pro 完成 30 场独立试验（每场最多 3 轮规划）；不修改游戏存档或榜单。

```sh
node --env-file=.env scripts/benchmark-play-effort.mjs --run
node scripts/summarize-play-effort.mjs --write-report
```

原始记录写入忽略提交的 `.artifacts/effort-benchmark/results.json`，包含真实动作和数值用量，不包含凭据或思考正文。已有结果时脚本拒绝覆盖，避免把复测偷偷替换成最好的一次。只有 30 场完整结束并覆盖全部题目和模式组合，才允许生成完整报告。首步统计排除撞墙，通关由生产游戏引擎确认；失败和服务异常分别保留。

[2026-09-13 真实对比](docs/PLAY-EFFORT-BENCHMARK-2026-09-13.md)：高思考 9/10 通关、首动中位数 80.70 秒；低思考 10/10、61.26 秒；关闭思考 0/10。这是旧整段规划协议的历史结果。用户现已选择默认无思考，4473/4474 的新试玩和挑战均使用逐步推动协议，出题保持 high。

## 自己设计像素宠物

在自己的宠物卡片点击“编辑外观 / 导入导出”。可以选预设、改名、调色，用画笔和橡皮画 16×16 像素，也可撤销和恢复预设。

- JSON 保存名字、类型和每个像素，导出后可以完整导入；不会复制账号、积分、排名、关卡或密钥。
- PNG 导出 16×16 透明图，支持导入 16–256 像素的正方形 PNG，按最近邻缩成 16×16。半透明像素以透明度 128 为阈值转换。
- 导入先预览，点击保存才应用。只允许修改自己的宠物；旧存档没有外观字段时自动使用原种类预设。

## 文件结构

```text
public/             页面、双棋盘、外观编辑器（原创 SVG / Canvas 像素角色）
shared/game.mjs     同一套移动、回放、求解、生成与计分规则
shared/pet.mjs      像素预设、安全渲染与外观文件校验
server/             HTTP、访客身份、持久化、宠物竞技、后台任务、模型适配器
test/               规则、真实 HTTP 链路、外部模型模拟、页面事件回归
docs/               任务约定与验收记录
data/               本地运行存档，忽略上传
```

## 当前限制与待办

- 本地 Node 使用单进程 JSON 存档，不要让多个进程共用同一个 `DATA_DIR`；线上 Sites 使用 D1，两个环境的存档不会自动互通。
- 手机登录与游客绑定已接入；尚未绑定的游客 Cookie 丢失后不能保证恢复所有权。存档与积分不随外观 JSON 导出。
- 尚无完整反刷分、容量压测、自动归档和运维保障；服务端动作验证不能证明操作者一定是真人。
- 用户最新决定是每日签到赠送 0.5 元站内 AI 使用额度。独立参考模块已有测试，但签到页面、D1 幂等账本及真实模型用量扣减尚未接入，不属于 v11。广告、充值和微信支付暂缓。
- 新功能交付需要分别说明源码、自动测试、真实模型实玩和线上发布状态，不能用旧验收记录证明新版本全部行为。

部署与身份边界见 [Sites 说明](docs/SITES.md)，历史本地原型记录见 [验收记录](docs/ACCEPTANCE.md)。


## 拳台攻防调整（已整合）

新拳赛保留 30 基础血量和真人对手 5 倍数值。J 轻拳伤害 3，K 重拳 6，I 抱摔 4；L 防住普通拳不掉血，抱摔无视防御但距离短、起手慢，可被轻拳打断或后退躲开。重拳打空/被防住后不能取消收招，攻击收招中的对手额外造成 2 × 攻击倍率伤害。画面显示打空、防御和反击提示。旧拳赛保留原规则，历史成绩不改写。

Node 和 Sites 共用新版规则与模型说明；模型可读取有限的过去出招/结果，不读取未来输入。此次浏览器使用算法陪练验证，未进行真实 DeepSeek 新版博弈验收；代码已合入 main，由统一发布任务同步到现有站点。详见 [调整记录](docs/BOXING-BALANCE-2026-09-14.md)。

## 出题能力与困难关（2026-09-14）

宠物通过不同的推箱子关卡后，出题能力自动升级：Lv.1 为 8×8 / 2 箱，累计 3 张升 Lv.2（9×9 / 3 箱），累计 10 张升 Lv.3（10×10 / 4 箱）。仅服务端验证的宠物通关计入；重复通关、聊天、出题证明和真人通关不增加此进度。旧存档沿用已有通关记录。

在宠物卡片选择「地图大小」「箱子数量」「难度」，然后后台备新题。解锁后仍可选较小规格，升级不自动替换当前守擂关。困难模式要求最少推箱次数至少为 3 × 箱子数 + 2；不达标或验证超预算时保留旧关。模型收到规格与不达标反馈，最多修复一次，不会冒用算法题作为模型结果。

Node / Sites 共用规则；规格随备题任务固定，当前比赛继续使用原有关卡版本。存档使用现有宠物 JSON，无须新增数据库表。实现、样本和验收边界见 [出题能力说明](docs/PUZZLE-MASTERY-2026-09-14.md)。

## 小精灵命名与后续横版玩法

页面默认名称统一为“小精灵”，旧存档名称中包含的旧称也转换显示；内部种类 ID 和历史存档保留。首页、小院、对话作者、外观编辑、比赛及介绍页使用同一名称。历史聊天和小院动态中的旧称统一转换显示，不改写保存的历史文本。此修改已发布到现有 Sites v15，14 项相关回归测试及构建通过。

后续横版学习玩法的已确认设计：玩家和小精灵同关行动，玩家负责示范，不能收集金币或触发机关；只有小精灵的实际行动能获得金币、开启机关。横版大模型示范学习已实现于源码，完整玩家风格训练仍未实现。
