# 多任务开发与合并

## 唯一源码和最新基线

GitHub `FromNextWorld/PetRival` 的 `main` 是合并后的统一基线。根目录包含本地 Node 与线上 Sites 实现；`sites-app/` 仅用于发布，`.artifacts/` 是历史交付或实验文件，不能作为后续开发起点。

每次开始修改前检查实际目录、当前分支和 `git status`，执行 `git fetch origin`，检查 `origin/main` 的最新提交。文档中记载的发布提交只用于追溯，不能替代这次检查。

## 独立开发

1. 保留现有未提交文件，先确认其所属任务，不能通过 reset、clean 或覆盖旧文件来取得干净目录。
2. 新任务从最新 `origin/main` 创建自己的 `codex/` 分支和独立 worktree。路径及分支名使用任务实际名称，不能重复占用别人的工作区。
3. 已有功能分支先保存自己的工作，再合入最新 `origin/main` 并检查冲突；不要在共享目录直接切换分支。
4. 无法建立独立目录时，先与整合任务约定文件归属，避免同时编辑相同文件。交付后冻结文件，直到整合完成。

## 交付和合并

交付应包含基线提交、功能提交或精确差异、文件清单、测试结果、待办和限制。整合任务审查冲突、保留各任务功能，运行与变化有关的验证，再合入 main。禁止把旧副本整目录覆盖到新源码。

功能影响前后端、Node 与 D1 时，需要逐项说明两种运行环境是否都已实现。数据库变化必须包含增量迁移；不得修改已应用的旧迁移或清空玩家存档。

代码变化同步更新 README 和对应 docs。GitHub 推送和网站发布是两个步骤：由统一发布任务同步精确源码到已有 Sites 仓库，再构建和发布，核对最终成功状态。纯文档修订不改变线上游戏运行版本。

## 当前交付边界（2026-09-14）

- 游戏整合提交 `b995ae8` 对应线上 v11：同页推箱子/打拳、比赛技能槽、推箱子反馈优化、手机号登录、小院和对话。
- 候选预演已经接入代码，默认仍为 `feedback`；未启用 `preview` 不代表漏合并。
- 每日签到 0.5 元 AI 额度只有独立参考模块，签到 UI、D1 幂等账本及真实用量扣减待接入。广告、充值、微信支付暂缓，不能描述成已上线。
- 181 项整合自动测试通过；真实模型及本机拳台卡顿的验证边界见 [整合记录](docs/claims/2026-09-14-multi-game-integration.md)。
